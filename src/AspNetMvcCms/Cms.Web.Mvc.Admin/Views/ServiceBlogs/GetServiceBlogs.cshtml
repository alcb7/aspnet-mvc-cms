@using Cms.Data.Models.Entities;
@model List<ServiceBlogEntity>

<div class="page-wrapper">
    <div class="content">
        <div class="row">
            <div class="col-sm-5 col-5">
                <h4 class="page-title">Service Blogs</h4>
            </div>

        </div>
        <div class="row">
            <div class="row">

                <div class="col-sm-4 col-8 text-right m-b-30">
                    <a asp-action="AddServiceBlogs" asp-controller="ServiceBlogs" class="btn btn-primary btn-rounded float-right"><i class="fa fa-plus"></i> Add Doctor</a>
                </div>


            </div>
            <div class="col-md-12">
                <div class="table-responsive">
                    <table class="table table-striped custom-table mb-0 datatable">
                        <thead>
                            <tr>

                                <th>ID</th>
                                <th>Title</th>
                                <th>Description</th>
                              
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Id</td>
                                    <td>@item.Title</td>

                                    <td>@item.Description</td>
                                  

                                    <td class="text-right">
                                        <div class="dropdown dropdown-action">
                                            <a href="#" class="action-icon dropdown-toggle" data-toggle="dropdown" aria-expanded="false"><i class="fa fa-ellipsis-v"></i></a>
                                            <div class="dropdown-menu dropdown-menu-right">
                                                <a class a="dropdown-item" asp-action="DeleteContacts" asp-controller="Contact" asp-route-id="@item.Id" data-toggle="modal" data-target="#delete_doctor"><i class="fa fa-trash-o m-r-5"></i> Delete</a>
                                                <a class="dropdown-item" asp-action="UpdateServiceBlogs" asp-controller="ServiceBlogs" asp-route-id="@item.Id"><i class="fa fa-pencil m-r-5"></i> Edit</a>
                                                @*    <a method="post" asp-action="DeleteContacts" asp-controller="Contact" asp-route-id="@item.Id">
                                            <input type="hidden" name="id" value="@item.Id" />
                                            <button type="submit">Sil</button>
                                            </a> *@
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>