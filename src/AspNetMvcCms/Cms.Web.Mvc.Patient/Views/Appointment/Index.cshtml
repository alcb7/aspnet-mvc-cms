
<form>
    <label for="departmentSelect">Departman Seçin:</label>
    <select id="departmentSelect" name="department">
        <option value="1">Bölüm 1</option>
        <option value="2">Bölüm 2</option>
        <!-- Diğer departmanlar için gerekli option değerlerini ekleyin -->
    </select>

    <label for="doctorSelect">Doktor Seçin:</label>
    <select id="doctorSelect" name="doctor">
        <!-- Seçilen departmana göre JavaScript ile doldurulacak -->
    </select>
</form>
<script>
    // Departman seçimi değiştiğinde doktorları güncelle
    document.getElementById('departmentSelect').addEventListener('change', function() {
      var departmentId = this.value;
      var doctorSelect = document.getElementById('doctorSelect');

      // İlgili departmana ait doktorları almak için bir AJAX çağrısı yapabilirsiniz
      // Örnek bir AJAX çağrısı:
      // $.ajax({
      //   url: '/Appointment/GetDoctorsByDepartment',
      //   method: 'POST',
      //   data: { departmentId: departmentId },
      //   success: function(doctors) {
      //     doctorSelect.innerHTML = '';
      //     doctors.forEach(function(doctor) {
      //       var option = document.createElement('option');
      //       option.value = doctor.Id;
      //       option.text = doctor.Name;
      //       doctorSelect.appendChild(option);
      //     });
      //   }
      // });

      // AJAX çağrısı kullanmıyorsanız, sabit bir veri kümesi ile örnek yapabilirsiniz
      var doctors = [
        { id: 1, name: 'Doktor 1' },
        { id: 2, name: 'Doktor 2' },
        // Diğer doktorlar için gerekli bilgileri ekleyin
      ];

      doctorSelect.innerHTML = '';
      doctors.forEach(function(doctor) {
        var option = document.createElement('option');
        option.value = doctor.id;
        option.text = doctor.name;
        doctorSelect.appendChild(option);
      });
    });
</script>